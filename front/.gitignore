# env
.env
# Vite's per-developer overrides. To point the dev server at another backend, put
# VITE_BACKEND_URL in .env.development.local -- it wins over .env.development, which
# .env.local does not. These describe one machine, so they are never committed.
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Lock files - use npm (package-lock.json) only
yarn.lock

# Test results
test-results/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.iml
*.idea
*.xml